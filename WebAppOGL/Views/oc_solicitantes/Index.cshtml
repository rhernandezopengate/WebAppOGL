@model IEnumerable<WebAppOGL.Entities.OrdenesCompra.oc_solicitantes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutOrdenesCompras.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreCompleto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.adm_empleados_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.oc_supervisores.NombreCompleto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.adm_empleados_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.oc_supervisores.NombreCompleto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
